import React from "react";

const contentStyle = {
    backgroundColor: '#dd3344',
    height: '500px',
};

const Content = () => {
    return (
        <div style={contentStyle}>
            / ---------- Content goes here ----------- /
        </div>
    );
}

export default Content;